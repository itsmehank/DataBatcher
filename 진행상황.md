이 프로젝트 진행상황
- v5가 최신 버전
- 이를 기반으로 진행? 일단 이정도 계획이면 충분한지 물어본 상황


전체 진행 상황
- 내 카톡으로 보낸 pdf 파일을 기반으로 매일 훈련을 진행할 예정
- 다만 이 훈련과 관련해서 필요한 기능들을 여기나 grafana에 추가할 예정
- 대상 종목 추리기 혹은 특정 날짜 시장 상황 판별하기 등등에 대한 기능
- 알람 기능도 추가했으면

---
현재 진행단계

적재 테스트까지 완료. RUN_GUIDE_KO.md를 기반으로 실행 가능

테스트로 실행해서 데이터 적재함. 이를 바탕으로 grafana 대시보드 구성작업 진행예정

테이블 관련 정보는 해당 md 파일에 정리했음
- /Users/hank.es/PythonProject/DataBatcher/docs/database_schema.md
- 이를 기반으로 grafana 대시보드 구성하고, 알람도 설정해보기
- 이후 작업은 etf 알람 설정
- 서버 구축 후 grafana 주소 만들기.


------

## [수정 필요 로직]

[ 해결 완료 ] daily_update.py 에서의 EMA 계산 과정
- daily_update에서는 약 250일분 데이터를 읽어와서(stock_prices) EMA를 계산 
  - 이 경우, 실제 EMA와 차이 발생 (EMA는 누적해서 영향을 미치기 때문)
  - EMA의 경우, stock_indicators 테이블과 stock_prices 테이블을 join 해서 읽기
  - join 해서 읽은 테이블에서 date 기준으로 가장 최근 40 rows만 읽기
  - 이 데이터에서 가장 최근 30 rows 부터 EMA 컬럼에 값이 있는지 없는지 확인
    - 값이 없는 EMA 컬럼이 있는 경우, EMA 계산 후 해당 컬럼만 업데이트
    - 값이 없는 EMA 컬럼이 없는 경우, 해당 날짜는 계산하지 않고 Pass
- bulk_update.py의 경우, 전체 데이터를 읽어와서 순차적으로 계산하기 때문에 이상이 없음
- [ 결론 ] EMA의 경우, 현재 약 250일분을 읽어서 계산하는데 이를 500일정도로 늘렸음
- 이렇게 늘리면 오차가 매우 줄어들고 대부분의 상용 서비스에서도 이러한 방식으로 계산
- EMA를 정확히 계산하려면 일단 전체 데이터를 대상으로 수행하거나 이전 EMA 값을 가지고 계산하는
- 로직을 직접 만들어야하는데 이렇게 되면 pandas api를 사용하지 못하기 때문에 반복문 수행 
- 시간이 많이 소요됨
- 오차가 작기 때문에 window를 늘리는 방식으로 대응



## [ 테이블 관리 스크립트 만들기 ]

[ 해결 완료 _ 테이블_작업_스크립트_가이드.md 참고]
테이블 전체 삭제하기 스크립트

테이블 내용만 삭제하기 스크립트

테이블에서 특정 symbol에 대한 row 모두 삭제하기 스크립트

테이블에서 특정 symbol, 특정 colume의 값에 해당하는 row만 삭제하기



## [ 라이브러리 변환 작업 _ 작업 완료 ]
- FinanceDataReader → pykrx 전환 완료


## ✅ [ stock_prices 테이블 적재 시 currency 필드가 비어 있음 _ 작업 완료 ] - RESOLVED (2026-01-28)
- pykrx는 currency 정보를 제공하지 않으며, 한국 주식은 모두 KRW이므로 currency 컬럼 제거
- 완료 사항:
  - ✅ 스키마 파일(01_schema.sql) 수정 - currency 컬럼 제거
  - ✅ 문서(database_schema.md) 업데이트 - currency 컬럼 제거 및 source 'pykrx' 반영
  - ✅ ALTER TABLE 쿼리 작성 (scripts/migrations/remove_currency_column.sql)
  - ✅ 코드 검증 완료 - currency를 참조하는 Python 코드 없음 확인


## [ 데이터 적재 시 현재 source가 여전히 FDR로 작성되고 있음 __ 작업 완료]
- stock_indicators 테이블 적재 시 source 컬럼의 값이 여전히 FDR로 작성됨
- stock_prices 테이블의 경우에는 제대로 작성되고 있음 (source = pykrx)



## [ 데이터 적재 시 date 컬럼의 값이 yyyy-mm-dd 형태로 작성되도록 하기 __ 작업 완료 ]
- 현재는 2025-03-06 09:00:00와 같은 형태로 적재되고 있음
  - stock_prices, stock_indicators 모두 이러한 형태로 적재됨 (수정 필요) 



## [주봉 데이터 증분 업데이트 과정 확인 _ 확인 완료]
- 한국 주식, 크립토, 미국 주식 주봉 증분 업데이트시, 지표 계산에서 과거 몇주를 가져오는지 확인 필요


## [ indicator 추가 작업 _ 현재 계획서만 작성한 상황 (아래 작업 끝내고 진행할 예정)]
- 계획서: indicator_backfill_plan.md
- 설정 인자
  - 소스 테이블, 적재 테이블, market, symbol
  - 소스 테이블 기본값: stock_prices
  - 적재 테이블 기본값: stock_indicators
  - market 기본값: KOSPI
  - symbol 기본값: 전체
  - 기간의 경우 각 symbol별로 전체 기간으로 설정 → 전체 데이터를 가져와서 작업

- 데이터 update시, 기존에 date, symbol, indicator, market 값이 동일하면 해당 row는 pass(update 하지 않음)
- 현재 대화 작성중
- 네가 요구한 데이터 베이스에 있는 테이블 스키마 정보를 제공할게. 이부분을 확인하고 필요하다면 계획을 구체화해줘.
추가로 원본 데이터를 읽어올 때 중복 데이터가 있는 경우 어떻게 해야할지 물어보는거라면, 그럴 경우는 없겠지만 만약 있다면 둘 중 하나의 데이터만 사용하는 것으로 해줘.
'weekly_update는 저장 시 source='FDR' 라고도 쓰고요(혼재). '의 경우.... 



## [ 일봉 지표 계산 시 ]
- 현재는 none 값 존재 row에 대해서는 insert 하지 않음
- 원래 주봉도 동일했음
- 하지만 이번에 주봉의 경우 변경 예정 (일봉도 동일하게 변경 필요)
- 지표 없는 경우에도 (값이 none) 해당 row를 적재
- 이렇게 안하면, 데이터 부족으로 무조건 none 값인 지표를 가진 주차는 영원히 계산 대상
- 물론 이후 모든 지표 값이 존재하는 주차가 나오면 그 이전 주차들은 순회하기 않겠지만
- sma_200 지표가 있는 현재로서는 200주가 지나기전까지 200번 계산을 매주해야함


## [ 주봉 데이터 적재시 source 컬럼 제거 ]
- 주봉 가격 테이블(stock_prices_weekly)과 주봉 지표 테이블(stock_indicators_weekly)에 모두 해당하는 내용
- 이미 적재된 일봉 테이블을 읽어서 해당 데이터를 사용하기때문에 따로 데이터 소스를 작성할 필요 없음
- 따라서 주봉 데이터 적재시 source 컬럼 제거 작업은 필요 없기에 해당 로직 제거
- 추가로 테이블 초기화 작업(init_db.py)에서도 해당 컬럼을 제거해줘.
- 또한 테이블 스키마 설명 문서(docs/database_schema.md)에서도 해당 내용(주봉 테이블에서 source 컬럼 제거)를 반영해줘.
- 마지막으로 현재 docker로 실행중인 Mysql을 root 계정으로 접근해서 기존 주봉 테이블에서 source 컬럼을 제거해줘. 


## market 컬럼의 값이 다른 문제
- 현재 stock_prices 테이블에서는 모든 한국 시장 종목에 대해서 market 컬럼의 값을 XKRX(한국거래소)로 작성한다.
- 하지만 stock_indicators 테이블에서는 코스피 종목에 대해서 market 컬럼의 값을 KOSPI로 작성한다.
- 동작 원리는 아래와 같다. (bulk, daily 동일)
  - --market KOSPI 입력받으면 symbol_master 테이블에서 market 필드의 값이 KOSPI인 종목 목록 추출
  - 이렇게 추출한 목록(종목 코드 목록)을 입력 인자로 사용해서 pykrx 메소드 호출 
  - pykrx에서는 제공하는 모든 종목에 대해서 market 값을 XKRX로 반환하고 이를 그대로 저장하는것
- [ 확인 필요 ] weekly 적재 작업에서도 --market 값을 받는데, 이 역시 위와 동일하게 symbol_master 테이블을 통해 조회 대상을 추출하는데 사용되는지 확인
  - 만약 symbol_master를 거치지 않고 바로 stock_prices 테이블에서 market 필드의 값이 KOSPI인 것을 조회하는데 사용된다면 문제 발생
    - 해당 조건을 충족시키는 데이터가 없기 때문에 조회 되는 데이터가 없어서 문제가 발생 가능
- 이 이슈에 대해서는 아래 방식으로 접근 필요
  - market 컬럼의 값이 필수 인자인지 확인
  - 필수 인자인 경우 해당 인자 값으로 market 컬럼의 값을 채우는 로직으로 변경
    - all 인자인 경우, 먼저 symbol_master에서 DISTINCT(market)으로 market 필드값 목록을 추출하고,
    - 각 인자에 대해서 작업을 수행하는 방식으로 market 컬럼의 값을 채우는 방식 적용


## [ 다트 관련 라이브러리 작업 필요 ]
- CAN SLIM 중 반영 가능한 일부 반영하기 위해서 작업 필요
- 해당 작업의 경우 '다트분석.md' 파일을 참고해서 계획부터 세워서 진행하기로 함 (중간에 계속 테스트도 포함시켜서 진행하도록함)


## [ 코스피, 코스닥, ETF에 대해서도 적재 대상에 추가 ]
- ETF는 완료
- 시장 지수는 아직 미완료


## [ 금, 은, 구리, 팔라듐, S&P500과 같은 대상도 적재 대상에 추가 _ 완료 ]



## [ 암호화폐 적재 대상으로 추가 _ 완료]


## [ 나스닥, 뉴욕증권거래소 적재 대상 추가 _ 완료 ]


## [ 작업 가이드 문서 이름 정리하기 & 작업 가이드 문서 디렉토리 만들기 (통합)]
- 가이드 문서를 토대로 database table 스키마 정보 문서 업데이트하기

## [ GCP 이전 조사 진행중 ]
- VM을 사용하지 않고 SaaS를 사용하는 방식
- /Users/hank.es/PythonProject/DataBatcher/진행중md파일/GCP이전_조사~.md

## [ GitHub Action → 성공/실패 결과를 MySQL에 기록 ]

- 예: 실행 시간 , 성공 종목 수 , 실패 종목 수
- 나중에 문제 생겼을 때 원인 추적이 엄청 쉬워짐


## [ 심볼 정보에 섹터 정보 포함시켜서 적재하기 ]
------
## [ 아이디어 ]

symbol_prices, symbol_indicators 테이블에서 date 컬럼의 표현 방식을 yyyy-mm-dd로 변경하기
(현재는 시간까지 표현되어 있음)

--

RS (상대강도) 지표 역시 indicator 테이블에 생성하기


--

주봉 테이블 적재 작업 추가하기
주봉 작업 역시 두 단계로 진행 (다만 데이터 소스가 FDR이 아닌 Table)
- (1) 테이블 전체를 읽고 주봉 테이블 생성 -> 주봉 테이블 전체를 읽고 indicator 테이블 업데이트
- (2) 가장 최근 365일 데이터를 읽고 주봉 테이블 업데이트 -> 주봉 테이블에서 250주 읽고 가장 최근 주부터 indicator 생성
  - 생성 후 주봉 indicator 테이블에 update -> 이 때, date, symbol 기준으로 해당 row 존재하면 update 하지 않고 작업 모두 종료
- 주봉 테이블의 경우 config/settings.yaml 별도의 indicator 설정을 작성할 수 있도록 구성


주봉 테이블 기준 view 생성 추가하기
- init_db.py에 추가



----

[ 상대 강도 테이블 생성 작업 ]

상대 강도 아이디어는 해당 문서에 정리
 

             